# yaml-language-server: $schema=https://schema.blue-build.org/modules/dnf-latest.json

type: dnf
repos:
  copr:
    - errornointernet/walker
    - yalter/niri
install:
  packages:
    - elephant
    - mpc
    - mpv
    - mpv-mpris
    - nautilus
    - ncmpcpp
    - niri
    - playerctl
    - walker
    - waybar
