# yaml-language-server: $schema=https://schema.blue-build.org/modules/dnf-latest.json

# Module: Gaming
# Steam and universal gaming packages

type: dnf
repos:
  nonfree: rpmfusion
install:
  packages:
    - steam
    - steam-devices
    - gamescope
    - mangohud
    - gamemode
    - vulkan-loader
    - vkBasalt
    - libxcrypt-compat
