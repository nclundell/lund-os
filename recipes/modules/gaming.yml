# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    repos:
      nonfree: negativo17
      cleanup: true
    install:
      packages:
        - gamemode
        - gamescope
        - mangohud
        - protontricks
        - steam
        - steam-devices
