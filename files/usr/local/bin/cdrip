#! /usr/bin/env bash

case $1 in
  ogg | mp3 | flac | opus)
    abcde -o $1
    ;;
  *)
    echo "File format \"$1\" not recognized."
    echo "Defaulting to OGG format..."
    abcde -o ogg
    ;;
esac
